name: "PR Labeler"
on:
  pull_request:
    types: [opened]

jobs:
  pr-labeler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v2
        env:
          repo-token: ${{ secrets.GITHUB_TOKEN }}